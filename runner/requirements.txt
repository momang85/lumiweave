# AI Agent Hub Runner — 命令行工具依赖
# Python >= 3.10

# 核心
pyyaml>=6.0            # YAML 解析
pydantic>=2.0          # 数据校验

# LLM API
openai>=1.30           # OpenAI SDK（云端推理，含 DeepSeek 兼容）

# CLI 交互
rich>=13.0             # 终端美化输出（可选：缺失时自动降级纯文本）
